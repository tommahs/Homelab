apiVersion: v1
kind: Secret
metadata:
  name: tempo-secret
type: Opaque
stringData:
  TEMPO_READ_ENDPOINT: "tempo-read.example.com"
  TEMPO_WRITE_ENDPOINT: "tempo-write.example.com"
  TEMPO_S3_BUCKET_NAME: "tempo-traces"
  TEMPO_S3_ENDPOINT: "tempo.example.com"